services:
  - type: web
    name: discord-clone
    runtime: python
    plan: free
    buildCommand: bash build.sh
    startCommand: bash start.sh
    envVars:
      - key: DEBUG
        value: "True"
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: discord-clone-db
          property: connectionString

databases:
  - name: discord-clone-db
    plan: free
